#include <stdio.h>

/**
 * main - entry point
 *
 * Return: alway returnd 0
 *
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	putchar('\n');
	return (0);
}
